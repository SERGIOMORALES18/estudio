{
  "name": "velvet-backend",
  "version": "0.1.0",
  "description": "Backend for Velvet Club - serves frontend and mock API",
  "main": "src/server.js",
  "scripts": {
    "setup": "node scripts/check-install.js",
    "prestart": "npm run setup",
    "start": "node src/server.js",
    "predev": "npm run setup",
    "dev": "nodemon src/server.js",
    "seed": "node scripts/seed_profiles.js",
    "migrate": "node scripts/migrate_photos.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "morgan": "^1.10.0",
    "multer": "^2.1.0",
    "mysql2": "^3.2.0",
    "supertest": "^7.2.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  },
  "engines": {
    "node": ">=14"
  }
}
